import { topInfo, topNav } from '../topBar';

export function MiningScene() {
    k.scene("mining", () => {
        // Title text
        
        bar = topNav()

        info = topInfo()
        k.onUpdate(() => {

        })
    })
}