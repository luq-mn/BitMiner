import { topInfo, topNav } from '../topBar';import { topNav } from '../topBar';

export function ExchangeScene() {
    k.scene("exchange", () => {
        // Title text
        
        bar = topNav()
        info = topInfo()
    })
}